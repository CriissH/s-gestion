{
  "name": "saldo",
  "private": true,
  "version": "1.0.7",
  "scripts": {
    "prepare-frontend": "node scripts/prepare-frontend.js",
    "tauri": "tauri",
    "tauri:dev": "npm run prepare-frontend && tauri dev",
    "tauri:build": "npm run prepare-frontend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.2.0",
    "@tauri-apps/plugin-updater": "^2.9.0"
  }
}
